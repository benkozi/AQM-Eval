name: aqm-eval
channels:
  - conda-forge
  - ufs-community # Needed for SRW context
  - nodefaults
dependencies:
  - awscli=2.28.*
  - mypy=1.17.*
  - nco=5.3.*
  - pre-commit=4.3.* # dev only
  - pydantic=2.11.*
  - pytest=8.4.* # dev only
  - pytest-mock=3.14.* # dev only
  - python=3.11
  - ruff=0.12.* # dev only
  - typer=0.16.*
  - dask=2024.2.1
  - melodies-monet=1.*
  - uwtools # Needed for SRW context
